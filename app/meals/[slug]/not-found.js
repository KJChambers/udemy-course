export default function MealNotFound() {
    return (
        <main className="not-found">
            <h1>404: Meal Not Found</h1>
            <p>That sounds yummy... but we don't have it yet!</p>
        </main>
    );
}